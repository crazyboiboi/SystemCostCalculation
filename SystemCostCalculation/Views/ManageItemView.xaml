<UserControl x:Class="SystemCostCalculation.Views.ManageItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SystemCostCalculation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1632">

    <UserControl.Resources>
        
    </UserControl.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
          
            
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 -50 0 0">
            <StackPanel Margin="0 0 0 10" Orientation="Horizontal">
                <TextBlock Text="Code" Margin="0 0 20 0"/>
                <TextBox Width="100" Margin="0 0 20 0"/>
            </StackPanel>

            <StackPanel Margin="0 0 0 10" Orientation="Horizontal">
                <TextBlock Text="Name" Margin="0 0 20 0"/>
                <TextBox Width="175" Margin="0 0 20 0"/>
            </StackPanel>

            <StackPanel Margin="0 0 0 10" Orientation="Horizontal">
                <TextBlock Text="Category" Margin="0 0 20 0"/>
                <ComboBox Width="150" Margin="0 0 20 0"/>
            </StackPanel>

            <StackPanel Margin="0 0 0 10" Orientation="Horizontal">
                <TextBlock Text="Type" Margin="0 0 20 0"/>
                <TextBox Width="100" Margin="0 0 20 0"/>
            </StackPanel>

            <StackPanel Margin="0 0 0 10" Orientation="Horizontal">
                <TextBlock Text="Description" Margin="0 0 20 0"/>
                <TextBox Width="175" Margin="0 0 20 0"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.RowSpan="2">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                <TextBlock Text="Sort By"/>
                <ComboBox Width="150" Margin="10 0 0 0"/>
                <TextBlock Text="Find" Margin="40 0 0 0"/>
                <TextBox Width="175" Margin="10 0 0 0"/>
            </StackPanel>

            <DataGrid x:Name="stockItemTable"  CanUserAddRows="False" CanUserReorderColumns="False" CanUserResizeColumns="False" CanUserResizeRows="False" AutoGenerateColumns="False" Margin="0 10 0 0" Height="500" >
                <DataGrid.Columns >
                    <DataGridTextColumn IsReadOnly="true" Header="ItemID" Binding="{Binding }" Width="0.25*"/>
                    <DataGridTextColumn IsReadOnly="true" Header="Description" Binding="{Binding}" Width="*"/>
                    <DataGridTextColumn IsReadOnly="true" Header="Size" Binding="{Binding }" Width="0.5*"/>
                    <DataGridTextColumn IsReadOnly="true" Header="Type" Binding="{Binding }" Width="0.5*"/>
                    <DataGridTextColumn IsReadOnly="true" Header="Cost" Binding="{Binding }" Width="0.5*"/>
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>
            



        </Grid>

</UserControl>
